/* Write a function `round` that takes a number as argument. Return the number rounded to nearest whole number. */
function round(num) {
    return Math.round(num)
}

console.log(round(2.5)); // -> 3
console.log(round(2.1)); // -> 2
console.log(round(2.7)); // -> 3